=============================================================================

history : show history of latest command in terminal

history | grep httpd : filter history by "httpd"

=============================================================================

Kill a port in ubuntu

sudo kill -9 $(sudo lsof -t -i:9001)

=============================================================================
